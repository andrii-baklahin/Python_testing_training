# -*- coding: utf-8 -*-
from selenium.webdriver.firefox.webdriver import WebDriver
import unittest


def is_alert_to_alert(wd):
    pass

class TestAddGroup(unittest.TestCase):

    def setUp(self):
        self.wd = WebDriver()
        self.implicity_wait(60)

    def test_add_message(self):
        wd = self.wd
        wd.get()
